#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 *
 * Return: Always 0.
 */eturn: 1 if c is uppercase, otherwise return 0.
 */
int _isupper(int c)
{
	if (c.isupper)
		return (1);
	else
		return (0);
}
